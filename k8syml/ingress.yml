apiVersion: extensions/v1beta1
kind: Ingress
metadata:
 name: static-ingress
 namespace: upright
spec:
 rules:
 - host: upright.lekkalahyma.xyz
   http:
     paths:
     - path:
       backend:
         serviceName: static-svc
         servicePort: 80