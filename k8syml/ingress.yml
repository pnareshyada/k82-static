apiVersion: extensions/v1beta1
kind: Ingress
metadata:
 name: static-ingress
 namespace: application
spec:
 rules:
 - host: application1.ajendra.xyz
   http:
     paths:
     - path:
       backend:
         serviceName: static-svc
         servicePort: 80